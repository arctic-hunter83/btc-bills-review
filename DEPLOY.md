# الرفع على GitHub و Deno Deploy

منصّة مطابقة فواتير الاتصالات — إدارة المنشآت التعليمية · قسم الهندسة والصيانة ·
مجموعة الهندسة الإلكترونية.

الباقة كاملة وجاهزة. لا تحتاج تثبيت أي شيء على جهازك، وكل الخطوات من المتصفح —
بنفس الطريقة التي رفعت بها نظام سجل المكالمات.

---

## ١. أنشئ المستودع على GitHub

1. افتح <https://github.com/new>.
2. **Repository name:** `batelco-invoice-platform`
3. اختر **Private** — الباقة تحتوي أسماء موظفين وأرقام حسابات فوترة.
4. لا تضع علامة على `Add a README file` (الباقة فيها README).
5. اضغط **Create repository**.

## ٢. ارفع الملفات

في صفحة المستودع الفارغ:

1. **uploading an existing file** → أو `Add file` ← `Upload files`.
2. اسحب **محتويات** مجلد الباقة (لا المجلد نفسه):
   `main.ts` · `deno.json` · `README.md` · `DEPLOY.md` · `.gitignore`
   ومجلدَي `static/` و `seed/` بكامل ما فيهما.
3. اكتب في `Commit changes`: `النسخة الأولى من المنصّة`.
4. اضغط **Commit changes**.

> رفع المجلدات بالسحب يحافظ على مسارها. تأكد بعد الرفع أن الشجرة هكذا:
> ```
> main.ts
> deno.json
> static/app.html
> seed/staff.json
> seed/purchase-orders.json
> ```

## ٣. اربطه بـ Deno Deploy

1. افتح <https://dash.deno.com> وسجّل الدخول بحساب GitHub نفسه.
2. **New Project** ← **Deploy from GitHub repository**.
3. اختر `batelco-invoice-platform` والفرع `main`.
4. **Install Step:** اتركه فارغاً.
5. **Entrypoint:** `main.ts`
6. اضغط **Deploy**.

خلال ثوانٍ يعطيك رابطاً مثل
`https://batelco-invoice-platform.<اسمك>.deno.net`

## ٤. أول دخول

- افتح الرابط، وسجّل الدخول برقمك الشخصي وكلمة السر الأولية
  (`Moe@` متبوعة برقمك الشخصي).
- **المنصّة تطلب منك تغييرها فوراً** — غيّرها قبل أي شيء آخر.
- بلّغ كل موظف بأن يفعل نفس الشيء عند أول دخول.

## ٤ب. تحقّق أول تشغيل — دقيقتان

`main.ts` لم يُشغَّل على Deno من قبل (بيئة العمل التي بُني فيها لا تحتوي Deno).
البناء سليم نحوياً والواجهة مختبَرة بالكامل ضد خادم بنفس عقد الـ API، لكن أول
تشغيل حقيقي هو هذا. امشِ على هذه النقاط بالترتيب:

| # | تحقّق | المتوقع |
|---|---|---|
| ١ | افتح الرابط | تظهر شاشة الدخول بشعار الوزارة وختم المجموعة |
| ٢ | افتح `/healthz` | `{"ok":true}` |
| ٣ | ادخل برقمك الشخصي وكلمة سر خاطئة | «الرقم الشخصي أو كلمة السر غير صحيحة» |
| ٤ | ادخل بكلمة السر الصحيحة | تدخل، ويظهر اسمك أعلى الصفحة |
| ٥ | انظر شريط أمر الشراء | الأمر <span dir="ltr">21121040</span> نشط · ٢٣ حساباً · ٧٤ خطاً · مُرِّر ‎22,075.223‎ |
| ٦ | لوحة أوامر الشراء | خمسة أوامر، و١٤ فاتورة في «فواتير مُرِّرت قبل المنصّة» |
| ٧ | ارفع فواتير شهر واحفظه ثم حدّث الصفحة | الشهر باقٍ في السجل |

**إن فشلت الخطوة ١ أو ٢** فالخطأ غالباً في البذر (قراءة مجلد `seed/`). افتح في
Deno Deploy: `Logs`، وانسخ لي أول سطر خطأ — وغالباً يكون أن مجلدَي `seed/`
و`static/` لم يُرفعا بمسارهما، فتأكد من شجرة الملفات في الخطوة ٢ أعلاه.

**إن دخلت لكن الشاشة فارغة من أوامر الشراء**، فملف `seed/purchase-orders.json`
لم يُقرأ — نفس السبب.

## ٥. التحديثات لاحقاً

ادخل المستودع ← افتح الملف المطلوب ← أيقونة القلم ← عدّل ← `Commit changes`.
Deno Deploy ينشر التعديل تلقائياً خلال ثوانٍ. لا حاجة لأي أمر في الطرفية.

---

## ماذا تغيّر عن الصفحة المستقلة

| | الصفحة المستقلة | المنصّة على Deno |
|---|---|---|
| التحقق من كلمة السر | داخل المتصفح | **على الخادم** — لا تصل المتصفح |
| مكان كلمات السر | داخل ملف الصفحة | Deno KV، مشتقّة PBKDF2 بملح لكل حساب |
| الجلسة | تخزين المتصفح | كوكي `HttpOnly` + `Secure` + `SameSite=Lax` |
| الصلاحيات | فحص في الواجهة | **فحص على الخادم في كل طلب** |
| البيانات | متصفح واحد | مشتركة بين كل الأجهزة والموظفين |
| محاولات الدخول | بلا حد | ٨ محاولات كل ١٥ دقيقة لكل رقم |

الملفات (فواتير الشهر) ما زالت **تُقرأ داخل متصفحك ولا تُرفع للخادم**. الذي يُحفظ
على الخادم هو نتيجة المطابقة فقط: الإجماليات والفروق وحالة التمرير.

---

## ملاحظات تشغيلية

**النسخ الاحتياطي.** Deno KV مُدار، لكن اجعل تنزيل الـ CSV الشهري عادة — فهو نسخة
احتياطية مقروءة خارج المنصّة.

**البذر يحدث مرة واحدة.** عند أول تشغيل والقاعدة فارغة تُنشأ الحسابات السبعة وأوامر
الشراء من مجلد `seed/`. بعدها لا يُقرأ المجلد إطلاقاً — التعديلات من داخل المنصّة.
إعادة الرفع لا تمسح شيئاً.

**النطاق الخاص.** إن أردت عنواناً تحت نطاق الوزارة، من Deno Deploy:
`Settings` ← `Domains` ← `Add Domain`، ويحتاج سجلاً من إدارة نظم المعلومات.

**البيئة المحلية (اختياري).** إن أردت تجربتها على جهازك قبل الرفع:
```bash
deno task dev      # ثم افتح http://localhost:8000
```
