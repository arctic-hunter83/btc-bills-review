# الرفع — نسخة الملف الواحد

منصّة مطابقة فواتير الاتصالات · مجموعة الهندسة الإلكترونية.

كل شيء داخل `main.ts`: الخادم والواجهة وبيانات البذر. **رفعة واحدة، ملف واحد.**
نفس طريقتك في سجل المكالمات.

---

## ١. المستودع

1. <https://github.com/new> ← الاسم `batelco-invoice-platform` ← **Private**.
2. لا تضع علامة على `Add a README file`.
3. **Create repository**.

## ٢. ارفع الملف

`uploading an existing file` ← اسحب `main.ts` وحده ← `Commit changes`.

## ٣. Deno Deploy

1. <https://dash.deno.com> ← **New Project** ← **Deploy from GitHub repository**.
2. اختر المستودع والفرع `main`.
3. **Install Step:** فارغ · **Entrypoint:** `main.ts`
4. **Deploy**.

## ٤. تحقّق أول تشغيل — دقيقتان

`main.ts` لم يُشغَّل على Deno من قبل (بيئة البناء لا تحتوي Deno). البناء سليم
نحوياً والواجهة مختبَرة بالكامل ضد خادم بنفس عقد الـ API، لكن أول تشغيل حقيقي
هو هذا. امشِ بالترتيب:

| # | تحقّق | المتوقع |
|---|---|---|
| ١ | `/healthz` | `{"ok":true}` |
| ٢ | الصفحة الرئيسية | شاشة الدخول بشعار الوزارة وختم المجموعة |
| ٣ | رقمك الشخصي + كلمة سر خاطئة | «الرقم الشخصي أو كلمة السر غير صحيحة» |
| ٤ | كلمة السر الصحيحة (`Moe@` + رقمك) | تدخل ويظهر اسمك أعلى الصفحة |
| ٥ | شريط أمر الشراء | `21121040` نشط · ٢٣ حساباً · ٧٤ خطاً · مُرِّر `22,075.223` |
| ٦ | لوحة أوامر الشراء | ٥ أوامر · ١٤ فاتورة في «فواتير مُرِّرت قبل المنصّة» |
| ٧ | ارفع شهراً واحفظه ثم حدّث الصفحة | الشهر باقٍ في السجل |

إن وقف شيء: Deno Deploy ← `Logs` ← انسخ لي أول سطر خطأ.

## ٥. أول ما تدخل

- **غيّر كلمة سرك** من زر «كلمة السر» أعلى الصفحة، وبلّغ الزملاء يفعلوا مثلك.
  الاصطلاح `Moe@` + الرقم الشخصي متوقَّع، وأي زميل يعرف رقم زميله يعرف دخوله.
- **أدخل سقف كل أمر شراء** من لوحة أوامر الشراء (زر «السقف» أو «تعديل») ليظهر
  المتبقي. جدول تسليم الفواتير لا يحتوي سقوف أوامر الهواتف، فتُركت فارغة عمداً
  ولم تُقدَّر.

## ٦. التحديثات لاحقاً

المستودع ← `main.ts` ← أيقونة القلم أو `Upload files` بنسخة جديدة ←
`Commit changes`. Deno Deploy ينشر خلال ثوانٍ.

> **لا تحرّر `main.ts` يدوياً لتغيير الواجهة.** الواجهة مضمَّنة كنص داخل الملف
> ومولَّدة من مصدر المنصّة. أي تعديل على الشكل أو المنطق يُطلب مني فأعيد توليد
> الملف كاملاً.

---

## ما بداخل الملف

| | |
|---|---|
| الخادم | Deno KV · جلسات · صلاحيات · حد لمحاولات الدخول |
| الواجهة | `APP_HTML` — المنصّة كاملة، ٢٦٤ كيلوبايت |
| البذر | ٧ حسابات (بلا مفاتيح — تُشتق عند أول تشغيل) · ٥ أوامر شراء |
| الحجم | ٣٥٠ كيلوبايت |

**البذر يعمل مرة واحدة فقط** عند قاعدة بيانات فارغة. إعادة رفع الملف لا تمسح
حساباً ولا شهراً ولا أمر شراء.

## الأمن

- كلمات السر: PBKDF2-SHA256 ١٢٠ ألف دورة بملح لكل حساب، **على الخادم**، ولا
  تصل المتصفح إطلاقاً.
- الاشتقاق يجري حتى لحساب غير موجود كي لا يكشف زمن الرد وجوده، والمقارنة ثابتة
  الزمن.
- الجلسة كوكي `HttpOnly; Secure; SameSite=Lax` لمدة ١٢ ساعة.
- كل طلب مغيِّر يتطلب ترويسة `X-Requested-With: batelco-platform`.
- ٨ محاولات دخول فاشلة لكل رقم في ١٥ دقيقة ثم حظر مؤقت.
- تغيير رقم شخصي أو حذف حساب يُبطل جلساته فوراً.
- `PUT /api/state` لا يقبل الحسابات إطلاقاً — تُدار عبر `/api/users` وحدها، فلا
  يستطيع طلب أن يرقّي صلاحية نفسه.
- **ملفات الفواتير لا تُرفع للخادم** — تُقرأ في المتصفح، ويُحفظ ناتج المطابقة فقط.
